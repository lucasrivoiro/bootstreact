import { PlaceholderSizeType } from './types';

export interface PlaceholderProps {
    size?: PlaceholderSizeType;
    number?: number;
}

export interface PlaceholderInterface {
    placeholderSize?: PlaceholderSizeType;
    numberOfPlaceholders?: number;
}
